public class Die
{
  private int numDots;
  
  // Sets numDots to a random integer from 1 to 6
  public void roll(int dSize)
  {
    numDots = (int)(dSize * Math.random()) + 1;
  }

  // Returns numDots
  public int getNumDots()
  {
    return numDots;
  }
}